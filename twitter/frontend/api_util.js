const APIUtil = {
  followUser: id => {

  },

  unfollowUser: id => {
    // return (followData) => {
    //   this.followState = "false";
    //   this.$el.attr("data-initial-follow-state", "false");
    //   this.render();
    // };
  }
};

module.exports = APIUtil;
